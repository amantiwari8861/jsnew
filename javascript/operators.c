#include<stdio.h>
int main()
{
    // identifiers: name must be relatable
    /*
    int num ber;//no space is allowed
    int 1num;//numeric digit at starting is not allowed
    int float;//must not be reserved keywords (32)
    int num$;//no special character is allowed except _ (underscore)
    int Aman_Tiwari;//valid
    */
    //Arithematic operators (+,-,*,/,%(modulus) )
    int result=10%3;
    printf("the remainder is %d \n",result);
    return 0;
}